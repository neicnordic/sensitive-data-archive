{{- if or (or (eq "all" .Values.global.deploymentType) (eq "external" .Values.global.deploymentType) ) (not .Values.global.deploymentType) }}
{{- if .Values.global.downloadV2.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "sda.fullname" . }}-download-v2
  labels:
    app: {{ template "sda.fullname" . }}-download-v2
spec:
  ports:
  - name: download-v2
    port: 80
    targetPort: download-v2
    protocol: TCP
  {{- if eq "prometheus" .Values.global.observability.OTEL_METRICS_EXPORTER }}
  - name: prometheus
    port: {{ default 9464 .Values.global.observability.OTEL_EXPORTER_PROMETHEUS_PORT }}
    targetPort: prometheus
    protocol: TCP
  {{- end }}
  selector:
    app: {{ template "sda.fullname" . }}-download-v2
{{- end }}
{{- end }}
