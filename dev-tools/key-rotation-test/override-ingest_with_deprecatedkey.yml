services:
  ingest:
    depends_on: !reset []
    volumes:
      - ./config-ingest_with_deprecatedkey.yaml:/config.yaml
  verify:
    depends_on: !reset []
    volumes:
      - ./config-ingest_with_deprecatedkey.yaml:/config.yaml
  mapper:
    depends_on: !reset []
    volumes:
      - ./config-ingest_with_deprecatedkey.yaml:/config.yaml
  finalize:
    depends_on: !reset []
    volumes:
      - ./config-ingest_with_deprecatedkey.yaml:/config.yaml
  api:
    depends_on: !reset []
    volumes:
      - ./config-ingest_with_deprecatedkey.yaml:/config.yaml
  reencrypt:
    depends_on: !reset []
    volumes:
      - ./config-ingest_with_deprecatedkey.yaml:/config.yaml
  download:
    depends_on: !reset []
    volumes:
      - ./config-ingest_with_deprecatedkey.yaml:/config.yaml
  rotatekey:
    depends_on: !reset []
    volumes:
      - ./config-ingest_with_deprecatedkey.yaml:/config.yaml